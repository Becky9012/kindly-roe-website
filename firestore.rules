rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all client access - all writes go through Admin SDK via Cloud Functions
    match /{document=**} {
      allow read, write: if false;
    }
  }
}

